// API Endpoint to endpoints living in website API
const siteEndpoint = 'http://localhost:2080'

// API endpoint to buffet bot specific requests
const botEndpoint = 'http://localhost:1080'

export { siteEndpoint, botEndpoint }
